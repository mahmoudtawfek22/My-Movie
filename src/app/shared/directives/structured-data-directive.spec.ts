import { StructuredDataDirective } from './structured-data-directive';

describe('StructuredDataDirective', () => {
  it('should create an instance', () => {
    const directive = new StructuredDataDirective();
    expect(directive).toBeTruthy();
  });
});
