import { MaxNumOfLettersPipe } from './max-num-of-letters-pipe';

describe('MaxNumOfLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxNumOfLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
